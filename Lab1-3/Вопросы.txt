1.Этот запрос выводил только актёров БЕЗ подчинённых
SELECT actor_name
FROM actors
WHERE connect_by_isleaf =1
CONNECT BY PRIOR actor_id=boss_id
START WITH boss_id IS null;

2. «Этот запрос позволяет рассчитать общий бюджет фильмов, отсортированные по годам» 
Если изменить порядок сортировки, в стольбце SUM мы не получим общий бюджет до этого времени (этого года)
Потому что, 